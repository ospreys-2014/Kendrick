<%= form_for :review, url: reviews_path do |f| %>
   <p>
    <%= f.label :review %><br>
    <%= f.text_area :body %><br>
    <%= f.hidden_field :artist_id, { :value => @user.id } %>

    <%= f.submit %>  </p>

<% end %>
